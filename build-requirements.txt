# Build-time dependency for packaging the desktop app into a Windows .exe.
#   pip install -r build-requirements.txt
#   pyinstaller --noconfirm DTT-Platform.spec   ->  dist/DTT-Platform.exe
pyinstaller>=6.0
